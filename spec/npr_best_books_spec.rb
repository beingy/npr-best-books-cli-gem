require 'spec_helper'

describe NprBestBooks do
  it 'has a version number' do
    expect(NprBestBooks::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
